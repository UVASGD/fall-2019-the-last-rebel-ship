using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PowerupManager : MonoBehaviour
{

    public float[] shieldCapacity = { 100, 200, 500 };
    public float[] acceleration = { 5, 10, 30, 50, 100 };
    public float[] bulletSpeed = { 10, 20, 30 };
    public float[] gravityGun = { 50, 500, 1000 };
    public float[] tractorBeam = { 1 };

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }



}
